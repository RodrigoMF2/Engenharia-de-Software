const valorDefeito = 0
let resultadoCorrente = valorDefeito;

resultadoCorrente = (resultadoCorrente + 10) * 3 / 2 - 1 ;


let descriCalculo = `(${valorDefeito} + 
    
    
    10) * 3 / 2 - 1`;


outputResult(resultadoCorrente,descriCalculo);